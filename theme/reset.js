const blocks = {
  margin: 0,
  padding: 0
};

const headings = {
  'font-size': '100%',
  'font-weight': 'normal'
};

const list = {
  'list-style': 'none'
}
const form = {
  margin: 0
};

const boxSizing = {
  'box-sizing': 'border-box'
}

const media = {
  'max-width': '100%'
};

const iframe = {
  border: 0
};

const table = {
  'border-collapse': 'collapse',
  'border-spacing': 0
};

const tableElements = {
  padding: 0,
  'text-align': 'left'
};

const font = {
  family: {
    base: 'BlinkMacSystemFont, -apple-system, "Segoe UI", "Roboto", "Oxygen", "Ubuntu", "Cantarell", "Fira Sans", "Droid Sans", "Helvetica Neue", "Helvetica", "Arial", sans-serif'
  }
  size: {
    base: 16
  },
  "line-height": {
    base: 1.5
  }
}


export {blocks, headings, list, form, boxSizing, media, iframe, table, tableElements};
